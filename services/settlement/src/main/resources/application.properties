spring.application.name=settlement
server.port=${SERVER_PORT:8083}
spring.datasource.url=${SETTLEMENT_DATABASE_URL:jdbc:postgresql://localhost:5432/settlement_db}
spring.datasource.username=${SETTLEMENT_DATABASE_USERNAME:aven}
spring.datasource.password=${SETTLEMENT_DATABASE_PASSWORD:aven}
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
spring.flyway.enabled=true
spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer
management.endpoints.web.exposure.include=health,info
